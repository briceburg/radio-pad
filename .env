# Default to ephemeral ports (0) to avoid conflicts
RADIOPAD_REGISTRY_PORT=0
# Keep the web app on a stable localhost origin for Google OAuth web redirects.
RADIOPAD_REMOTE_CONTROL_PORT=5173
RADIOPAD_SWITCHBOARD_PORT=0
