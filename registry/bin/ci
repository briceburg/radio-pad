#!/usr/bin/env sh
set -eu

cd "$(dirname "$0")/.."
export UV_PROJECT_ENVIRONMENT="${UV_PROJECT_ENVIRONMENT:-tmp/.venv}"

uv run mypy src/ tests/
uv run ruff format --check src/ tests/
uv run ruff check src/ tests/
REGISTRY_PROFILES=api uv run pytest
